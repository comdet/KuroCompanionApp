<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Coarse location is enough for weather forecasts (city granularity);
         we use LocationManager.NETWORK_PROVIDER, not GPS. FYT7870 head unit
         and AOSP devices without GMS still expose this provider. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Shizuku — user grants this through the Shizuku Manager app once,
         lets us run shell-level commands (input keyevent VOLUME_UP/DOWN/MUTE)
         that the Chinese HUD ROM hooks at the InputDispatcher level.
         Optional: app degrades gracefully to AudioManager when Shizuku
         is unavailable or the permission hasn't been granted. -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:name=".CarCompanionApp"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_compass"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompanionAndroid"
        tools:targetApi="36">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.debug.SoulDebugActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:label="Soul Debug" />

        <activity
            android:name=".ui.debug.SensorDebugActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:label="Sensors" />

        <service
            android:name=".service.CarCompanionService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <service
            android:name=".service.OverlayService"
            android:exported="false" />

        <!-- Notification listener: captures Google Maps / Waze nav notifications.
             User MUST toggle this on in Settings → Apps → Special access →
             Notification access — there is no programmatic grant. -->
        <service
            android:name=".service.MapsNotificationListener"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Shizuku provider. The library declares its own ShizukuProvider
             with a different authority; we override the authority here so
             it's unique to our package and doesn't clash if the user has
             other Shizuku-enabled apps installed. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            tools:replace="android:authorities" />

    </application>

</manifest>
